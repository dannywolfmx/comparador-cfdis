name: comparador_cfdis
description: Aplicación para comparar CFDIs.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  xml: ^6.3.0
  file_picker: ^5.3.0
  path: ^1.8.3
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  provider: ^6.0.5
  open_file: ^3.3.2
  printing: ^5.11.0  # Asegurarnos de que es la última versión
  pdf: ^3.10.7
  # data_table_2: ^2.6.0 # Comentar o eliminar esta línea
  syncfusion_flutter_datagrid: ^25.1.37 # Añadir esta línea
  syncfusion_flutter_core: ^25.1.37 # Necesario para SfDataGrid
  pluto_grid: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # Quitar esta sección si está causando problemas
  # plugin:
  #   platforms:
  #     windows:
  #       pluginClass: none