name: comparador_cfdis
description: Aplicación para comparar CFDIs.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  xml: ^6.3.0
  file_picker: ^10.1.2
  path: ^1.8.3
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  provider: ^6.0.5
  open_file: ^3.3.2
  # data_table_2: ^2.6.0 # Comentar o eliminar esta línea
  syncfusion_flutter_datagrid: ^25.1.37 # Añadir esta línea
  syncfusion_flutter_core: ^25.1.37 # Necesario para SfDataGrid
  pluto_grid: ^8.0.0
  logger: ^2.5.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  uuid: ^4.2.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  bloc_test: ^9.1.4
  mocktail: ^1.0.0

flutter:
  uses-material-design: true
  # Quitar esta sección si está causando problemas
  # plugin:
  #   platforms:
  #     windows:
  #       pluginClass: none